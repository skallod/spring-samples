spring:
  application:
    name: rest-test

setting1: value1

management:
  endpoints:
    enabled-by-default: false
    web.exposure.include: health,prometheus
  endpoint:
    health.enabled: true
    prometheus.enabled: true
